image: python:3.6

stages:
        - lint
        - test

lint job:
        stage: lint
        before_script:
                - pip install -q pylint
        script:
                - pylint phs/
                - pylint examples/
                - pylint tests/

integration test parameter definition:
        stage: lint
        before_script:
                - pip install pytest
                - pip install pytest-cov
                - ./install.sh
        script:
                - pytest --disable-pytest-warnings --cov=phs --cov-branch tests/
